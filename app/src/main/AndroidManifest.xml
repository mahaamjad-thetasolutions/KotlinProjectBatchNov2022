<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thetapractice.KotlinProjectBatchNov2022">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinProjectBatchNov2022"
        tools:targetApi="31">
        <activity
            android:name=".FirebaseCRUDActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WebviewActivity"
            android:exported="true"/>
        <activity
            android:name=".StudentList.StudentListActivity"
            android:exported="true" />
        <activity
            android:name=".ConstraintLayoutActivity"
            android:exported="true" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCpDhqDxKCgBwDbOmeaMquteuAm_SN7WRs" />

        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ThetaBottomNavActivity"
            android:exported="true"
            android:label="@string/title_activity_theta_bottom_nav" />
        <activity
            android:name=".FragmentPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".DoctorList.DoctorListViewActivity"
            android:exported="true" />
        <activity
            android:name=".ImplicitIntentActivity"
            android:exported="true" />
        <activity
            android:name=".LifeCycleActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>